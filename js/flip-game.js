(()=>{const DURATION=45,COUNT=12,COOK=3.2,BURN=6.2;const grill=document.getElementById('flipGrill'),scoreEl=document.getElementById('flipScore'),timeEl=document.getElementById('flipTime'),msg=document.getElementById('flipMessage'),btn=document.getElementById('flipAction');let score=0,running=false,start=0,raf=0,meats=[];const now=()=>performance.now()/1000;function makeMeat(i){const el=document.createElement('button');el.className='flip-meat raw';el.type='button';el.setAttribute('aria-label',`烤肉 ${i+1}`);el.innerHTML='<span class="meat-face">🥩</span><small>生</small>';const m={el,side:0,sides:[0,0],last:now(),burned:false};el.onclick=()=>tap(m);grill.appendChild(el);return m}function resetMeat(m,t){m.side=0;m.sides=[0,0];m.last=t;m.burned=false;m.el.className='flip-meat raw';m.el.querySelector('small').textContent='生'}function updateSide(m,t){m.sides[m.side]+=Math.max(0,t-m.last);m.last=t}function stateOf(sec){return sec>=BURN?'burnt':sec>=COOK?'cooked':'raw'}function paint(m){const s=stateOf(m.sides[m.side]);m.el.className=`flip-meat ${s} side-${m.side}`;m.el.querySelector('small').textContent=s==='burnt'?'焦':s==='cooked'?'熟':'生'}function tap(m){if(!running)return;const t=now();updateSide(m,t);if(m.burned)return;const bothCooked=m.sides.every(x=>x>=COOK&&x<BURN);const currentBurnt=m.sides[m.side]>=BURN;if(currentBurnt){burn(m,t);return}if(bothCooked){score+=10;flash('+10 熟肉起鍋！');resetMeat(m,t);return}if(m.side===1&&m.sides.some(x=>x<COOK)){score-=5;flash('-5 還沒熟！');resetMeat(m,t);return}m.side=1-m.side;m.last=t;paint(m);m.el.classList.add('flipping');setTimeout(()=>m.el.classList.remove('flipping'),220)}function burn(m,t){if(m.burned)return;m.burned=true;score-=10;m.el.className='flip-meat burnt';m.el.querySelector('small').textContent='焦';flash('-10 烤焦了！');setTimeout(()=>{if(running)resetMeat(m,now())},650)}function flash(text){scoreEl.textContent=score;msg.textContent=text;msg.classList.remove('pop');void msg.offsetWidth;msg.classList.add('pop')}function loop(){if(!running)return;const t=now(),left=Math.max(0,DURATION-(t-start));timeEl.textContent=Math.ceil(left);for(const m of meats){if(m.burned)continue;updateSide(m,t);if(m.sides[m.side]>=BURN)burn(m,t);else paint(m)}if(left<=0){end();return}raf=requestAnimationFrame(loop)}function begin(){cancelAnimationFrame(raf);score=0;running=true;start=now();scoreEl.textContent='0';timeEl.textContent=DURATION;msg.textContent='點肉翻面，兩面熟了就起鍋！';btn.textContent='進行中…';btn.disabled=true;grill.innerHTML='';meats=Array.from({length:COUNT},(_,i)=>makeMeat(i));raf=requestAnimationFrame(loop)}function end(){running=false;cancelAnimationFrame(raf);btn.disabled=false;btn.textContent='再玩一次';msg.textContent=`時間到！本局 ${score} 分`;for(const m of meats)m.el.disabled=true}btn.onclick=begin;document.querySelectorAll('.mode-btn').forEach(b=>b.onclick=()=>{if(running)return;document.querySelectorAll('.mode-btn').forEach(x=>x.classList.toggle('active',x===b));const flip=b.dataset.mode==='flip';document.getElementById('timingGame').classList.toggle('hidden',flip);document.getElementById('flipGame').classList.toggle('hidden',!flip)})})();